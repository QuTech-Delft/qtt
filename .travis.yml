language: python
python:
    - "3.5"
branches:
  only:
    - master

# command to install dependencies
install:
    - pip install --upgrade pip
    - pip install ipython
    - pip install -r develop_requirements.txt
    - python setup.py develop
# command to run tests
script: 
    - python  qtt/test.py --skip-coverage


