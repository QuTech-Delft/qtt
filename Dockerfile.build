FROM ubuntu:bionic

RUN apt-get update && \
    apt-get install -y tzdata gedit python3 git-core python3-pip python3-pyqt5 \
                       python3-opencv python3-pytest x11-xserver-utils

RUN pip3 install setuptools

ENV MPLBACKEND Agg
ENV QT_QPA_PLATFORM offscreen
